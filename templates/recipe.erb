# ><%= "[ #{name} ]".center(75,'-') %><

@current_scroll = "<%= key %>"
@before_configs["<%= key %>"].call if @before_configs["<%= key %>"]
say_scroll '<%= name %>'

<%= config.compile if config %>
@configs[@current_scroll] = config

<%= template %>
